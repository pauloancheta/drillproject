<%#
  TODO
  ADMIN ONLY
  This form is used to create and edit Tags/tools/categories.
  Upon successful submission, it should redirect the admin to a list of existing Tags (tags#index?).
  Upon unsuccessful submission, it should display the flash[:alert] and re-render the submitted form.
%>
<%= simple_form_for @tag, class: "form-horizontal" do |f| %>
  <%= f.input :title %>
  <%= f.button :submit %>
<% end %>