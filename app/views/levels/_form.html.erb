<%#
  TODO
  ADMIN ONLY
  This form appears on an admin page TBD. It is used to create and edit levels (eg. Beginner, Intermediate, Advanced).
  Upon successful submission, it should redirect to a page displaying all the existing levels (levels#index?).
  Upon unsuccessful submission, it should display the flash[:alert]
%>
<%= simple_form_for @level, class: "form-horizontal" do |f| %>
  <%= f.input :title %>
  <%= f.button :submit %>
<% end %>