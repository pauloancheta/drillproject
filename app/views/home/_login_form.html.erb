<%= simple_form_for (user || User.new), url: sessions_path, method: :post, remote: true do |f| %>
<%= f.input :email %>
<%= f.input :password %>
<%= f.submit "Sign In", class: "logsubmit" %>
<% end %>