<%= simple_form_for @drill_group, class: "form-horizontal" do |f| %>
  <%= f.input :name %>
  <%= f.input :description, as: :text %>
  <%= f.association :levels, as: :radio_buttons, label_method: :title %>
  
  <%= f.button :submit %>
<% end %>